import Root, { buttonVariants, type ButtonProps, type ButtonSize, type ButtonVariant } from "./button.svelte";

export {
    type ButtonProps,
    type ButtonProps as Props,
    type ButtonSize,
    type ButtonVariant,
    buttonVariants,
    Root,
    //
    Root as Button
};
